[pytest]
pythonpath = .
testpaths = tests
# The live Groq call is opt-in: `pytest -m integration` runs it.
addopts = -m "not integration"
markers =
    integration: hits the live Groq API (needs GROQ_API_KEY)
